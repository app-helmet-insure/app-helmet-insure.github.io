import Axios from 'axios';

export const dodoswap = (data) => {
    let data = {
        fromTokenAddress: '', // sellAdress
        fromTokenDecimals: '', // sellDecimals
        toTokenAddress: '', // buyAdress
        toTokenDecimals: '', // buyDecimals
        fromTokenAddress: '', // sellAdress
        fromTokenAddress: '', // sellAdress 
    };
};
